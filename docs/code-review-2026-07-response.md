# 代码评审裁定书 — 对《code-review-2026-07.md》的核查与处置

> 核查日期：2026-07-06
> 方式：三组并行取证(代码证据组 / 依赖与构建组 / 文档一致性组),对评审 9 项主要发现逐项复核,并做评审未覆盖的独立扫描。
> 结论先行:9 项**全部属实或部分属实,无一凭空**;但其中 4 项的严重度或处置方向需要校准——"发现对、药方错"是本次裁定的主旋律。

---

## 一、9 项裁定表

| # | 原判 | 裁定 | 校准要点 |
|---|------|------|---------|
| 1 | Agent Loop 双系统重复(🔴) | ✅ 确认,处置不同 | deep-agents 实为**死代码**(仅测试引用,主流程从未接入)→ **已删除**,而非评审建议的"统一两套循环" |
| 2 | MCP Server 多数为 Stub(🔴) | ✅ 确认,处置不同 | MCP server 本就未接进应用运行时(仅供外部 MCP 客户端)→ **文档诚实化**(mcp-servers/README 标注 3 真实 + 6 stub,其余未审计),而非投入"真实现" |
| 3 | 子 Agent 无 LLM 推理(🟡) | ✅ 确认 | 全树核实:multi-agent 子 Agent **零 LLM 调用**,确为 Tool Wrapper;当前三路 LLM key 全挂,**换活 key 后再评估**是否加推理步骤 |
| 4 | Pipeline Middleware 未集成(🟡) | ✅ 确认 | 定义完整但从未接入执行链 → **middleware 已删除** |
| 5 | 后端无 TS 类型检查(🟡) | ✅ 确认 | tsc 46 错误基线**全部来自 src/**,且 CI 原本无 typecheck step → 已加 **server/tsconfig.json + CI typecheck step** |
| 6 | CORS 配置安全漏洞(🟡) | ⚠️ 代码属实,严重度夸大 | 单人本地工具、无部署面,CORS 宽松本身影响有限;真正的暴露面是 **0.0.0.0 监听** → 已修一行 CORS + LAN 白名单 |
| 7 | pgDatabase.ts 上帝文件(548 行) | ⚠️ 属实,排位失真 | 548 行仅列全仓**第 14 大文件**;真巨头是 backtestScreener.ts(2135 行)与 ScreenerView.tsx(1521 行)→ **不动**,拆它性价比排不上号 |
| 8 | 依赖放置错误 | ✅ 确认且更严重 | 不止"位置不对":server/ 实际**靠根 node_modules 解析生产依赖**,`npm install --omit=dev` 会直接崩 → **依赖已修正** |
| 9 | 文档与实现不同步 | ✅ 确认 + 精度修正 | 实质是**文档自相矛盾**(final-architecture 自称"Phase 1-7 完成" vs refactoring-roadmap 标注 Phase 3 已放弃),而非单向"声称完成" → **已同步**(2026-07-06 文档修订:banner + 技术栈 + MCP 状态表 + 工具数) |

---

## 二、独立新发现摘要(评审未覆盖)

| 编号 | 发现 | 处置 |
|------|------|------|
| F1 | `r2` / `clamp01` 等工具函数在 10 处重复实现 | ✅ 已修(收敛为共享实现) |
| F5 | 选股快照**部分降级时整体覆盖**好快照的 bug | ✅ 已修 |
| F6 | `server/lib/redis.ts` 死代码(从未被任何运行时代码接线) | ✅ 已删 |
| F7 | 三个从未被 import 的依赖:`@zilliz/milvus2-sdk-node` / `sql.js` / `vectra`(ioredis 随 F6 一并移除) | ✅ 已从 package.json 删除 |
| F9 | ScreenerView.tsx 1521 行,9 个战法卡片无共享壳组件 | 📋 进 backlog(不阻塞) |
| F10 | 测试缺口:agentLoop 核心循环无测试 | ✅ 已补 |

---

## 三、处置决定

1. **死代码一律删除,不封存**:deep-agents 双循环(#1)、pipeline middleware(#4)、redis.ts(F6)、三个未用依赖(F7)全部物理删除。理由:封存的"以后可能用"从未兑现过,留着只增加认知负担与评审噪音。
2. **执行评审建议的第①②③批**(依赖修正 + CORS + 文档同步;Agent Loop 处置 + 后端 TS 配置;MCP 处置),但按本裁定口径执行(删除/诚实化,而非统一/真实现)。
3. **明确不做**:Sentry 错误监控、CI/CD 部署流水线强化、子 Agent LLM 推理增强。理由:本项目是**单人本地工具**——无多用户、无部署面、无 on-call,错误监控与部署流水线的收益前提不存在;子 Agent 推理待 LLM key 恢复后按 token 成本与产出质量再评估(见 #3)。

---

*裁定完毕。原评审文档保留不动,作为对照。*
