node_modules/
dist/
*.log
*.err
.vite/
.runtime/
.claude/*
!.claude/skills/

# Environment variables (never commit secrets)
.env
.env.local
.env.*.local
server/.env

# Vector index data (generated, not source)
server/data/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo

# Generated daily screener snapshots (data, not source)
docs/screener/
docs/moneyflow/

# Daily research reports dropped by the user + LLM analyses (personal data, not source)
docs/research/

# Private indicator reverse-engineering evidence (personal/proprietary, local only)
docs/n/

# ---- Third-party source material and derived research (never public) ----
# 公众号原文/截图、作者原图、第三方课程材料、蒸馏出的 skill、反推因子证据。
# 全部为「不可公开的重要数据源」：本地保留，永不入库。
#
# 2026-09-11 修复：docs/翼形形态/ 是错字规则（翼 ≠ 妖），从未生效，
# 导致 1095 个第三方形态截图（228MB）被跟踪并推送到公开远端。
# 两处都保留，避免再被改错。
docs/妖股形态/
docs/翼形形态/

# 作者原图、逐 bar 证据、冻结行情 fixture、复现工具
docs/v13-research/

# 蒸馏出的分析 skill（claim ledger 复现了作者主张与阈值）
skills/analyze-huishou-trading/

# 第三方课程原始转录与派生模式
server/knowledge/teacher/

# 研究运行产物与交接文档
/huishou-research.html
/TASK_HANDOFF_*.md
# 整个目录：内部交接稿全部与策略/战法相关（first-board / relay / screener /
# dragon / breakout 等），公开仓库不应包含。原先只 glob 了 huishou|ynmc|v13 三类，
# 2026-09-11 改为整目录下架。
docs/task-progress/

# ---- Personal / non-source artifacts (never commit) ----
# Job-search resumes, career planning, interview cheatsheets
docs/career/
docs/hao/
docs/Xia_Zhengyan_*
docs/Zhengyan_Xia_*
docs/夏正彦*
docs/interview-cheatsheet-2026.*
docs/202682410*.docx*
docs/附件*
docs/电力交易岗*

# iFind capability-probe cache (temporal data downloads)
/ifind_*.csv
/ifind_*.json

# Backend/frontend runtime logs
/logs-*.out

# Recorded videos / QA preview screenshots
/video_*.mp4
.qa/

# One-off personal scripts (resume builders / workstation checks)
/check_agent_gw.py
/build_*resume.py
/build_power_trading_resume.py
/build_supabase_frontend_resume.py
/.codex_read_docs.py

# ---- Agent/tool run-time artifacts (never commit) ----
# agent recovery context, provider routing configs
docs/agent/
# workbuddy assistant memory
docs/.workbuddy-ai/
/.workbuddy-ai/

# Personal job-hunt workspace (resumes, search results)
docs/job-hunt-2026-09/

# Scratch files and caches
tmp/
__pycache__/

# Heavy generated limit-ladder analysis archives (per-stock research rows; snapshots, not source)
docs/ladder/**/analysis-limit-ladder-v6*.json
docs/ladder/**/evidence-limit-ladder-v6*.json

# ── 私有战法层的前端覆盖（superposition 工作树）────────────────────────────
# 这些路径的文件属私有层。公开仓库里不存在；启用私有层时从
# `wax-wane-strategy/src/**` 复制回来（零改写，保持相对 import 有效）。
# src/strategy-ui/index.tsx 通过 import.meta.glob 按名单挂载，缺失即回退占位。
src/views/ScreenerView.tsx
src/views/RotationView.tsx
src/views/ResearchPanel.tsx
src/views/TrackRecordPanel.tsx
src/components/HuishouResearchPanel.tsx
src/components/HuishouResearchPanel.css
src/components/HuishouMinuteChecklist.tsx
src/components/HuishouQuality.tsx
src/components/RotationRhythmSummary.tsx
src/components/RotationTempoGrid.tsx
src/hooks/useScreener.ts
src/hooks/useScreenerForward.ts
src/hooks/useRotation.ts
src/hooks/useRotationTempo.ts
src/utils/rotationBoardLinks.ts
src/utils/rotationRhythm.ts
src/utils/screenerScanWindow.ts
src/huishouResearchMain.tsx
