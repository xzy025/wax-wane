name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: lint + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            server/package-lock.json

      - name: Install root deps
        run: npm ci

      - name: Install server deps
        run: npm ci --prefix server

      - name: Lint (src/ + server/)
        run: npm run lint

      # server 全量类型检查(2026-07-06 引入,0 容忍);src/ 另有 46 个历史基线错误,清零前不进 CI 闸。
      - name: Typecheck (server/)
        run: npm run typecheck:server

      - name: Frontend tests (jsdom)
        run: npm test

      - name: Server tests (node)
        run: npx vitest run --config vitest.server.config.ts
